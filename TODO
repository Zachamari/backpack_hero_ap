- Mission instance only works on first run because it's being overwritten- figure out a way to differentiate the fourth instance specifically and use that one only
- Item instance likely doesn't work at all, figure out which one actually works
- Theory: Each concurrent instance is a different save file? (this is true)